name: Atelier Build

# reusable build workflow for one chunk of the discover matrix
# each cell is its own job so it renders as its own check run with live logs
# nesting lets the parent fan out past the 256 cell matrix cap

on:
  workflow_call:
    inputs:
      repository:
        description: Atelier repository
        type: string
        default: stepbrobd/atelier
      ref:
        description: Atelier repository Git ref
        type: string
        default: master
      cells:
        description: JSON matrix object for this chunk
        required: true
        type: string
      push:
        description: Push build results to the binary cache
        required: false
        default: true
        type: boolean
      rules:
        description: Atelier rule file whose substituters and trusted-public-keys feed nix.conf
        required: false
        default: atelier.toml
        type: string
      install-command:
        description: Shell command that installs Nix; empty uses the built-in installer
        required: false
        default: ""
        type: string
      extra-conf:
        description: Extra nix.conf lines appended after the required settings
        required: false
        default: ""
        type: string
      pre-install:
        description: Shell command run before Nix is installed on every cell
        required: false
        default: ""
        type: string
      post-install:
        description: Shell command run after Nix is installed and configured on every cell
        required: false
        default: ""
        type: string
      pre-push:
        description: Shell command run before the cache push, only when a push would happen
        required: false
        default: ""
        type: string
      post-push:
        description: Shell command run after the cache push, only when a push would happen
        required: false
        default: ""
        type: string
      push-command:
        description: Shell command that pushes build results; empty uses the built-in cache push
        required: false
        default: ""
        type: string
    secrets:
      ATTIC_TOKEN:
        required: false
      CACHIX_AUTH_TOKEN:
        required: false
      CACHIX_SIGNING_KEY:
        required: false
      NIKS3_TOKEN:
        required: false

permissions:
  contents: read

jobs:
  cell:
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(inputs.cells) }}
    name: ${{ matrix.label }}
    runs-on: ${{ matrix.runner }}
    # id-token lets the niks3 push mint a github oidc token; harmless for the
    # other backends. it only takes effect if the caller granted it too
    permissions:
      contents: read
      id-token: write
    env:
      INSTALLABLE: ${{ matrix.installable }}
      LABEL: ${{ matrix.label }}
      EVAL_ERROR: ${{ matrix.error }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Report Evaluation Failure
        if: ${{ matrix.installable == '' }}
        shell: bash
        run: |
          echo "::error::Evaluation failed for ${LABEL}"
          printf '%s\n' "${EVAL_ERROR}"
          exit 1

      - name: Set Up Nix
        if: ${{ matrix.installable != '' }}
        uses: $/.github/actions/atelier
        with:
          system: ${{ matrix.system }}
          install-command: ${{ inputs.install-command }}
          extra-conf: ${{ inputs.extra-conf }}
          rules: ${{ inputs.rules }}
          pre-install: ${{ inputs.pre-install }}
          post-install: ${{ inputs.post-install }}
          post-build-hook: ${{ inputs.push && inputs.push-command == '' && github.event.pull_request.head.repo.fork != true && ((vars.ATTIC_SERVER != '' && vars.ATTIC_CACHE != '') || vars.CACHIX_CACHE != '' || vars.NIKS3_SERVER != '') }}

      # the streamer runs as the runner user with the step env, which is what
      # keeps the secrets out of the root-run hook. it drains the spool the
      # hook fills and pushes while the build runs. its stdout lands in a log
      # file the final drain replays into the step log
      - name: Start Cache Streamer
        if: ${{ inputs.push && inputs.push-command == '' && matrix.installable != '' && github.event.pull_request.head.repo.fork != true && ((vars.ATTIC_SERVER != '' && vars.ATTIC_CACHE != '') || vars.CACHIX_CACHE != '' || vars.NIKS3_SERVER != '') }}
        shell: bash
        env:
          ATTIC_SERVER: ${{ vars.ATTIC_SERVER }}
          ATTIC_CACHE: ${{ vars.ATTIC_CACHE }}
          ATTIC_TOKEN: ${{ secrets.ATTIC_TOKEN }}
          CACHIX_CACHE: ${{ vars.CACHIX_CACHE }}
          CACHIX_AUTH_TOKEN: ${{ secrets.CACHIX_AUTH_TOKEN }}
          CACHIX_SIGNING_KEY: ${{ secrets.CACHIX_SIGNING_KEY }}
          NIKS3_SERVER: ${{ vars.NIKS3_SERVER }}
          NIKS3_TOKEN: ${{ secrets.NIKS3_TOKEN }}
        run: |
          set -euo pipefail
          # a missing root means setup could not find the atelier tree
          if [ -z "${ATELIER_ROOT:-}" ]; then
            echo "::warning::Skipping cache streamer: setup did not complete"
            exit 0
          fi
          python3 "${ATELIER_ROOT}/src/atelier/stream.py" --mode stream \
            > "${RUNNER_TEMP}/atelier-stream.log" 2>&1 &
          printf '%s' "$!" > "${RUNNER_TEMP}/atelier-stream.pid"

      - name: Build
        id: build
        if: ${{ matrix.installable != '' }}
        shell: bash
        run: |
          set -o pipefail
          nix build "${INSTALLABLE}^*" --no-link --print-build-logs 2>&1 | tee build.log

      # the push phase brackets pre-push, the push (built-in or custom), and
      # post-push. with built-in backends the streamer has already been
      # pushing during the build, and the final drain below re-pushes the
      # full spool plus the outputs' closure as the backstop, also on a
      # failed build (!cancelled) so partial results land on the cache. the
      # pre/post hooks keep their success-only timing unless the built-in
      # drain runs, and only for a real build failure (not a setup failure,
      # where no push happens).
      # each step is env-passed and not interpolated into the run body so it
      # cannot break out of the run
      - name: Pre Push Hook
        if: ${{ inputs.push && matrix.installable != '' && github.event.pull_request.head.repo.fork != true && inputs.pre-push != '' && (success() || (!cancelled() && steps.build.outcome == 'failure' && inputs.push-command == '' && ((vars.ATTIC_SERVER != '' && vars.ATTIC_CACHE != '') || vars.CACHIX_CACHE != '' || vars.NIKS3_SERVER != ''))) }}
        shell: bash
        env:
          PRE_PUSH: ${{ inputs.pre-push }}
          ATTIC_SERVER: ${{ vars.ATTIC_SERVER }}
          ATTIC_CACHE: ${{ vars.ATTIC_CACHE }}
          ATTIC_TOKEN: ${{ secrets.ATTIC_TOKEN }}
          CACHIX_CACHE: ${{ vars.CACHIX_CACHE }}
          CACHIX_AUTH_TOKEN: ${{ secrets.CACHIX_AUTH_TOKEN }}
          CACHIX_SIGNING_KEY: ${{ secrets.CACHIX_SIGNING_KEY }}
          NIKS3_SERVER: ${{ vars.NIKS3_SERVER }}
          NIKS3_TOKEN: ${{ secrets.NIKS3_TOKEN }}
        run: |
          set -euo pipefail
          bash -c "${PRE_PUSH}"

      - name: Push To Cache
        if: ${{ !cancelled() && inputs.push && inputs.push-command == '' && matrix.installable != '' && github.event.pull_request.head.repo.fork != true && ((vars.ATTIC_SERVER != '' && vars.ATTIC_CACHE != '') || vars.CACHIX_CACHE != '' || vars.NIKS3_SERVER != '') }}
        shell: bash
        env:
          BUILD_OUTCOME: ${{ steps.build.outcome }}
          ATTIC_SERVER: ${{ vars.ATTIC_SERVER }}
          ATTIC_CACHE: ${{ vars.ATTIC_CACHE }}
          ATTIC_TOKEN: ${{ secrets.ATTIC_TOKEN }}
          CACHIX_CACHE: ${{ vars.CACHIX_CACHE }}
          CACHIX_AUTH_TOKEN: ${{ secrets.CACHIX_AUTH_TOKEN }}
          CACHIX_SIGNING_KEY: ${{ secrets.CACHIX_SIGNING_KEY }}
          NIKS3_SERVER: ${{ vars.NIKS3_SERVER }}
          NIKS3_TOKEN: ${{ secrets.NIKS3_TOKEN }}
        run: |
          set -euo pipefail
          # a failed setup leaves no atelier checkout, skip rather than error
          if [ -z "${ATELIER_ROOT:-}" ]; then
            echo "::warning::Skipping cache push: setup did not complete"
            exit 0
          fi
          python3 "${ATELIER_ROOT}/src/atelier/stream.py" --mode final || echo "::warning::Cache push failed"

      # custom push replaces the built-in one (like install-command replaces the
      # installer): when set, the native Push To Cache above is skipped. the
      # attribute is in INSTALLABLE (job env) so the command can resolve its own
      # out-paths, e.g. nix build "$INSTALLABLE^*" --no-link --print-out-paths;
      # the cache vars/secrets are exported too so a custom push can reuse them
      - name: Push To Cache (Custom)
        if: ${{ inputs.push && inputs.push-command != '' && matrix.installable != '' && github.event.pull_request.head.repo.fork != true }}
        shell: bash
        env:
          PUSH_COMMAND: ${{ inputs.push-command }}
          ATTIC_SERVER: ${{ vars.ATTIC_SERVER }}
          ATTIC_CACHE: ${{ vars.ATTIC_CACHE }}
          ATTIC_TOKEN: ${{ secrets.ATTIC_TOKEN }}
          CACHIX_CACHE: ${{ vars.CACHIX_CACHE }}
          CACHIX_AUTH_TOKEN: ${{ secrets.CACHIX_AUTH_TOKEN }}
          CACHIX_SIGNING_KEY: ${{ secrets.CACHIX_SIGNING_KEY }}
          NIKS3_SERVER: ${{ vars.NIKS3_SERVER }}
          NIKS3_TOKEN: ${{ secrets.NIKS3_TOKEN }}
        run: |
          set -euo pipefail
          bash -c "${PUSH_COMMAND}"

      - name: Post Push Hook
        if: ${{ inputs.push && matrix.installable != '' && github.event.pull_request.head.repo.fork != true && inputs.post-push != '' && (success() || (!cancelled() && steps.build.outcome == 'failure' && inputs.push-command == '' && ((vars.ATTIC_SERVER != '' && vars.ATTIC_CACHE != '') || vars.CACHIX_CACHE != '' || vars.NIKS3_SERVER != ''))) }}
        shell: bash
        env:
          POST_PUSH: ${{ inputs.post-push }}
          ATTIC_SERVER: ${{ vars.ATTIC_SERVER }}
          ATTIC_CACHE: ${{ vars.ATTIC_CACHE }}
          ATTIC_TOKEN: ${{ secrets.ATTIC_TOKEN }}
          CACHIX_CACHE: ${{ vars.CACHIX_CACHE }}
          CACHIX_AUTH_TOKEN: ${{ secrets.CACHIX_AUTH_TOKEN }}
          CACHIX_SIGNING_KEY: ${{ secrets.CACHIX_SIGNING_KEY }}
          NIKS3_SERVER: ${{ vars.NIKS3_SERVER }}
          NIKS3_TOKEN: ${{ secrets.NIKS3_TOKEN }}
        run: |
          set -euo pipefail
          bash -c "${POST_PUSH}"

      - name: Upload Log
        if: ${{ always() && matrix.installable != '' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          # percent encoded attribute path
          name: ${{ matrix.log }}
          path: build.log
          if-no-files-found: ignore
